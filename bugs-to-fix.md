[x] There is an error: "found no layout file for "html" for kind "taxonomy"" and "found no layout file for "html" for kind "section"" refactor the code so that when going to the info page for an app (such as jellyfin)

[x] Change the categories on the home page such that it's in the middle of screen, with margin on both sides.

[x] Change the categories so that every row has at least 5 apps. For example, with productivity and utility since their width is 2 + 3 = 5, they should be on the same row. Do not hardcode these categories as this is subject to change.

[x] Make the "Show More" button dynamic, so it only appears if there is more than 5 apps in a category, and have the category expand downward when clicked.

[x] For each of the app info pages, add an "X" icon in the upper right that will take you back to the homepage. Also if the user clicks "Add" it should take them back to the homepage as well.

[x] Change how "screenshots" in the content is handled. If there is more than one screenshot, make it a carousel of pictures.

[x] Make it so that all app icons on the home page and on the app info page have a square black background behind them that has a border radius of 23px. That way, if an image is an PNG it looks consistent with the other icons

[x] Change the User install screen to be 4 columns with a gray divider between each step. Use the Figma design for reference: https://www.figma.com/design/UYPTO8kuDxbYQNaeY5gIuS/Frontend?node-id=53-20&t=X5LJMX6DT6GKanus-4
